fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
requests>=2.31          # Google OAuth + Supabase Storage

# --- Base de datos (la MISMA que panel-control) ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# --- Seguridad / auth ---
argon2-cffi>=23.1          # hashing de contraseñas (Argon2id, OWASP)
bcrypt>=4.1                # solo para verificar hashes viejos (legacy)
PyJWT>=2.8
email-validator>=2.1

# --- Panel administrativo montado en /panel (web-tienda/panel) ---
openpyxl>=3.1.5            # export Excel del catálogo
Pillow>=10.0               # imágenes de productos
truststore>=0.9
pyotp>=2.9                 # MFA/TOTP de administradores
qrcode>=7.4                # QR del MFA y del cobro POS
bleach>=6.1                # sanitizar HTML de descripciones

# --- Pagos ---
stripe>=8.0,<14  # OJO: sin techo entró una versión donde los objetos
                 # de Stripe dejaron de soportar .get() y se rompió
                 # el webhook (los pagos no se acreditaban nunca).

# --- Build de CSS (compilar .scss -> .css una vez) ---
libsass>=0.22
